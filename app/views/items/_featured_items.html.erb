<h4>Featured Items at GPBO</h4>
<% if @featured_items.empty?%>
  <p>There are no featured items in that category at this time.</p>
<%end%>

<table class="striped highlight">
  <thead>
    <th>Name</th>
    <th>Price</th>
  </thead>
  <% for item in @featured_items %>
    <tr class="<%= cycle('dg_list_line_odd', 'dg_list_line_even') %>">
      <td><%= link_to item.name, item_path(item) %></td>
      <td><%= number_to_currency(item.current_retail_price) %></td>

    </td>
    </tr>
  <% end %>
</table>
<p class="supertiny"></p>
<br />
<%# <%= will_paginate @featured_items previous_label: "Previous&nbsp;", next_label: "&nbsp;Next" %> 
<p>&nbsp;</p>